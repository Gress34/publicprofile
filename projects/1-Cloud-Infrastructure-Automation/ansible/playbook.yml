- name: Configure servers
  hosts: all
  tasks:
    - name: Install Apache
      become: yes
      apt:
        name: apache2
        state: present
    - name: Start Apache service
      become: yes
      service:
        name: apache2
        state: started
        enabled: yes